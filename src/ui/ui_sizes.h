#ifndef UI_SIZES_H
#define UI_SIZES_H

#include <lvgl.h>

// Standard gap applied to grid row/column spacing and container padding
static constexpr lv_coord_t UI_PAD_STD      = 5;

// Fixed-width column for textarea inputs in two-column input grids
static constexpr lv_coord_t UI_GRID_INPUT_W = 80;

#endif // UI_SIZES_H
